Place your birthday campaign image here as `birthday.jpeg`.

This file is referenced by:
  app/services/whatsapp_birthday.py → send_birthday_whatsapp()

The image must be publicly accessible via a URL for the AiSensy media payload.
If you are serving it from a CDN or cloud storage, update AISENSY_BIRTHDAY_MEDIA_URL in .env instead.
